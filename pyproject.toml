[project]
name = "python"
version = "0.1.0"
description = "Learning Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.1.1",
]

[dependency-groups]
dev = [
    "ruff>=0.12.1",
]
